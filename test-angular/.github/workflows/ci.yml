name: CI

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    container:
      image: node:20-alpine
    steps:
      - name: Install dependencies
        run: npm install
      - name: Lint
        run: npm run lint
