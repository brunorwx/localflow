name: CI

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    container:
      image: golang:1.22-alpine
    steps:
      - name: Vet
        run: go vet ./...
      - name: Build
        run: go build ./...
